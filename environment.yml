name: pytask-julia

channels:
  - conda-forge
  - nodefaults

dependencies:
  - python
  - pip
  - setuptools_scm
  - toml

  # Package dependencies
  - julia
  - pytask >=0.3
  - pytask-parallel >=0.3

  # Optional package dependencies
  - pyyaml

  # Misc
  - black
  - bumpversion
  - jupyterlab
  - pre-commit
  - pytest-cov
  - pytest-xdist
  - tox-conda

  - pip:
    - -e .
